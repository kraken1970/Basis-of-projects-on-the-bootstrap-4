$('.carousel').carousel({
  interval: 2000,
  // keyboard: false,
  // отключить управление клавиатурой
  // wrap:false
  // запрет автопрокрутки после окончантя цикла
});

$('#exampleModal').modal({
  // keyboard: false,
  //нет закрытия по esc
  // backdrop: "static"
  // не закроется по нажатию на пустую область
})